/**
 * Advert Status enum (FE dùng chung)
 */
export enum AdvertRequestStatus {
  PENDING = 'pending',
  RUNNING = 'running',
  DONE = 'done',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
  ACCEPTED = 'accepted',
}
